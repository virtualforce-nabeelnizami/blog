<%= form_for @profile, :html => {:multipart => true} do |f| %>
    <%# if @profile.errors.any? %>
        <%#*<div id="error_explanation">%>
          <%#*<h2><%= pluralize(@profile.errors.count, "error") %>
<%#prohibited this profile from being saved:</h2>%>

          <%#*<ul>%>
            <%# @profile.errors.full_messages.each do |msg| %>
                <%#*<li><%= msg %>
            <%# end %>
          <%#*</ul>%>
        <%#*</div>%>
    <%# end %>

    
    <div class="name">
      <%= f.label :adress %>
      
      <%= f.text_field :adress %>
    </div>
    <div class="name">
      <%= f.label :intro %>
      
      <%= f.text_area :intro %>
    </div>
    <div class="name">
      <%= f.label :photo %>
    <%= file_field_tag "profile[photo]", :onchange => "readURL(this)" %>
    </div>
      <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
<div class="uploaded_img"><img src="" id="image" style="width: 200px; height: 100px"/></div>


<script>
    function readURL(input) {
        if (input.files && input.files[0]) {
            var reader = new FileReader();

            reader.onload = function (e) {
                $('#image').attr('src', e.target.result);

            }

            reader.readAsDataURL(input.files[0]);
        }
    }


</script>