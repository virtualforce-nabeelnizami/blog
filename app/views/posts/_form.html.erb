<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% @post.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%#*<div class="name">%>
    <%#= f.label :name %>
    <%#= f.text_field :name %>
  <%#*</div>%>
  <div class="name">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>
  <div class="name">
    <%= f.label :content %>
    <%= f.text_area :content %>
  </div>
  <div class="name">
    <%= f.label :category %>
    <%#= f.text_field :category %>
     <%= f.select :category_id, Category.all.collect {|category| [ category.category_name, category.id ] } %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
